// Get mouse position
var mx = mouse_x;
var my = mouse_y;

// Check if mouse is over the button
is_hovered = (mx > x && mx < x + btn_width && my > y && my < y + btn_height);

// Check if clicked
if (is_hovered && mouse_check_button_pressed(mb_left)) {
    is_pressed = true;

show_debug_message("You did not wave back")
global.WaveBack=1
}